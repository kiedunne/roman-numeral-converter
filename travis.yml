language: ruby
rvm:
 - 2.2
 - jruby
install: ./install-dependencies.sh
script:
- bundle exec rspec spec/
before_install:
- gem update --system
- gem install bundler
deploy:
  provider: heroku
  api_key:
    secure:
  on:
    repo: kiedunne/roman-numeral-converter
